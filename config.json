{
    "threshold": 90,
    "kill": "tskill ethminer",
    "path": "mine.bat",
    "reboot": "shutdown -r -t 00",
    "restartApp": 120,
    "rebootRig": 360,
    "cmd": "nvidia-smi --query-gpu=utilization.gpu --format=csv,noheader"
}